/**
 * Active ERD workspace bootstrap.
 *
 * Bundled examples live in editor-sample-catalog.js. The application itself
 * starts from an empty project schema so sample data never becomes real
 * project state unless the user explicitly opens a sample.
 */
const schemaData = {
  main: {
    tabName: 'MAIN',
    icon: 'fa-solid fa-database',
    title: 'Empty MAIN Schema',
    tables: [],
    relations: []
  }
};
